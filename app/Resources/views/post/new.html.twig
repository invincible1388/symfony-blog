{% extends 'base.html.twig' %}

{% block body %}
    <h1>Post creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <p><ul class="list-unstyled list-inline">
        <li>
            <a class="btn btn-default" href="{{ path('post_index') }}"><i class="ion-ios-arrow-thin-left"></i> &nbsp;Back to the list</a>
        </li>
    </ul></p>
{% endblock %}
